services:
  zoraxy:
    image: zoraxydocker/zoraxy:${APP_TAG}
    container_name: zoraxy
    restart: unless-stopped
    ports:
      - "${EXPOSE_PORT:-80}:80"
      - "${EXPOSE_PORT_HTTPS:-443}:443"
    volumes:
      - ${INSTALL_DIR}/data/config:/opt/zoraxy/data
    cap_add:
      - NET_ADMIN
